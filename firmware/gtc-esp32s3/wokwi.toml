[wokwi]
version = 1
firmware = '.pio/build/esp32-s3-wokwi/firmware.bin'
elf = '.pio/build/esp32-s3-wokwi/firmware.elf'

# GTC_SERVER_HOST em src/config.h tem de ser o IP real do PC na rede local
# (ex.: 192.168.1.50) para o ESP32 simulado conseguir chegar ao backend
# através do Private IoT Gateway da extensão Wokwi.
